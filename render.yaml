services:
  - type: web
    name: captcha-solver
    env: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: python render_start.py
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHONUNBUFFERED
        value: true